# Secrets — KHÔNG BAO GIỜ commit
.env

# Runtime data của server — SQLite, notes, browser auth state
data/

# Python
__pycache__/
*.pyc

# Frontend (Phase 3)
web/node_modules/
web/dist/

# Companion app build (PyInstaller)
local-agent/dist/
local-agent/build/

# Config test local
.test-agent-config/

# Config deploy per-server (DOMAIN=...)
docker/.env

# Test nhắm server thật — chứa invite token thật, KHÔNG commit (repo public)
.test-ui/dod_remote_*
