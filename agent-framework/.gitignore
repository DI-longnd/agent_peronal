# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env
# Bản sao lưu .env cũng chứa API key — `.env` không khớp `.env.bak`, `.env.local`...
.env.*
!.env.example

# Browser storage state (chứa cookie/session đăng nhập thật — không commit)
.auth/
